---
import BaseLayout from "../../layouts/BaseLayout.astro";
import GitHubContributionsComponent from "../../components/GitHubContributionsComponent.astro";
import { getAllPortfolioItems } from "../../lib/contentful";
import type { PortfolioItem } from "../../lib/types/portfolioItem";

const portfolioItems = await getAllPortfolioItems();
---

<BaseLayout title="Portfolio" description="Take a peek at what I've been working on!">
  <div class="portfolio-index-page">
    <section class="portfolio-index-hero">
      <div>
        <p class="home-kicker">Portfolio</p>
        <h1>Projects, experiments and things I have been building.</h1>
        <p>
          A collection of selected work, practical builds and creative experiments that show how I approach software engineering, accessibility and user-centred problem solving.
        </p>
      </div>
    </section>

    <GitHubContributionsComponent />

    <section class="portfolio-project-list" aria-label="Portfolio projects">
      {portfolioItems &&
        portfolioItems
          .sort((a, b) => new Date(b.sys.publishedAt).getTime() - new Date(a.sys.publishedAt).getTime())
          .map((item: PortfolioItem) => (
            <article class="portfolio-list-item" itemscope itemtype="http://schema.org/CreativeWork">
              <div class="portfolio-list-item__topline">
                <h2>
                  <a href={`/portfolio/${item.slug}/`}>{item.title}</a>
                </h2>

                <div class="portfolio-list-item__badges">
                  {item.isInDevelopment && (
                    <span>In Development</span>
                  )}

                  {item.isIframe && (
                    <span>Live demo</span>
                  )}
                </div>
              </div>

              <p>{item.description}</p>

              <a class="portfolio-list-item__link" href={`/portfolio/${item.slug}/`}>
                View project
              </a>
            </article>
          ))
      }
    </section>
  </div>
</BaseLayout>
